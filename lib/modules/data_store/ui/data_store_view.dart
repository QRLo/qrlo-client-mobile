import 'package:flutter/material.dart';

class DataStoreView extends StatelessWidget {
  const DataStoreView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
