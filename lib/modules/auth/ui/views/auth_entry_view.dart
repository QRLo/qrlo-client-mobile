import 'package:flutter/material.dart';

class AuthEntryView extends StatelessWidget {
  const AuthEntryView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
